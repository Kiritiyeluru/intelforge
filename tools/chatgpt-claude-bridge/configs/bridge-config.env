# ChatGPT-Claude Bridge Enhanced Configuration
# Source this file or set these environment variables to customize behavior

# === CORE FEATURES ===
# Enable automatic archiving of tasks (true/false)
export ARCHIVE_ENABLED=false

# Enable automatic Claude execution after clipboard save (true/false)
export AUTO_RUN_CLAUDE=false

# === ENHANCED FEATURES ===
# Enable system notifications (true/false)
export ENABLE_NOTIFICATIONS=true

# Enable automatic cleanup of ChatGPT preambles (true/false)
export ENABLE_CLEANUP=true

# Enable safety check for sensitive data (true/false)
export ENABLE_SAFETY_CHECK=true

# === CUSTOM PATHS ===
# Custom target file location (optional)
# export CUSTOM_TARGET="$HOME/my-claude-tasks/input.md"

# Custom archive directory (optional)
# export CUSTOM_ARCHIVE_DIR="$HOME/my-claude-tasks/history"

# Custom bridge directory (optional)
# export CUSTOM_BRIDGE_DIR="$HOME/my-claude-bridge"

# === ADVANCED OPTIONS ===
# Custom Claude task mode (future feature)
# export CLAUDE_TASK_MODE="default"
